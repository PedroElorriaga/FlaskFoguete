CRIEI A BASE HTTP PARA EFETUAR OS TESTES DO JWT

CRIEI A TABELA JWTPROJECT.DB E COLOQUEI O SCRIPT DB
CREATE TABLE IF NOT EXISTS users (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	nome_usuario VARCHAR(255) NOT NULL,
	agencia INT NOT NULL,
	conta INT NOT NULL,
	email VARCHAR(255) NOT NULL,
	hash_senha VARCHAR(255) NOT NULL,
	criado_em TEXT NOT NULL
);

DENTRO DA PASTA SECURITY CRIEI O ARQUIVO JWT_HANDLE
E DENTRO DELE CRIEI UMA CLASSE QUE CARREGA OS METODOS DE ENCODAR
E DECODAR O JWT (O PARAMETRO DE EXP É PASSADO NO PAYLOAD)